const listItemProject = document.getElementById('Project');
const listItemAbout = document.getElementById('About');

const activeListItem = ()=>{
    listItemAbout.style.background = "var(--secondary-color)";
}